apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: frontend_hpa
spec:
  minReplicas: 2
  maxReplicas: 6
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: frontend_deployment
  metrics:
    - type: Resource
      resources:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
